import React from 'react';

const Expand = ({ isDiscount, }) => {
  // Get props

  // Check if isDiscount
  // Make html template
    // Enter Code
    // Show Items

  return (
    <div>
      {
        isDiscount ?
        <div>EnterCode</div> :
        <div className='show-items'>ShowItems</div>
      }
    </div>

  )
};


export default Expand;